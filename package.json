{
  "name": "balapp-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "buy": "SET DELAY=900 && SET PROD=true && SET MODE=buy && node index.js",
    "bal": "SET DELAY=900 && SET PROD=true && SET MODE=bal && node index.js",
    "dev": "SET DELAY=15000 && SET PROD=false && nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.9.0",
    "nodemon": "^2.0.20",
    "ws": "^8.10.0"
  }
}
